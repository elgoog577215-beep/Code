# Tasks: 温中 AI 诊断长期升级执行清单

本任务清单从长期 spec 拆出，用于后续持续执行。状态说明：

- `[x]` 已完成并验证
- `[~]` 已启动但仍需扩展
- `[ ]` 待执行

## Phase 0: Eval 与现状体检

- [x] 建立规则层 eval 测试框架。
- [x] 覆盖输出格式细节、差一位、样例过拟合。
- [x] 覆盖暴力复杂度、运行稳定性、通过后复盘。
- [x] 覆盖输入读取理解错误 `INPUT_PARSING`。
- [x] 覆盖初始状态/状态重置 `INITIAL_STATE` / `STATE_RESET`。
- [x] 覆盖重复元素场景 `DUPLICATE_CASE`。
- [x] 覆盖最大规模边界 `MAX_BOUNDARY`。
- [x] 覆盖局部修复回退 `PARTIAL_FIX_REGRESSION`。
- [x] 形成 10 条可复跑规则层 AI 诊断样例。
- [x] 扩展到 15-20 条可复跑 AI 诊断样例。
- [x] 输出当前 AI 诊断基线报告。

## Phase 1: 诊断底座

- [x] 扩展 `SubmissionAnalysisResponse`，支持版本、细标签、证据引用、不确定性。
- [x] 新增 `DiagnosisEvidencePackage`。
- [x] 新增 `DiagnosisEvidencePackageBuilder`。
- [x] 新增 `RuleSignalAnalyzer`。
- [x] 扩展 `DiagnosisTaxonomy` 细粒度标签。
- [x] 将规则信号合并进提交诊断结果。
- [x] 调整 `AiReportService`，让模型接收证据包与规则信号。
- [x] 为证据包构建器增加单元测试。
- [x] 为旧 `reportJson` 增加兼容测试。
- [x] 将历史提交摘要接入证据包。

## Phase 2: Agent 编排链

- [x] 新增 `DiagnosticAgentService`，统一编排证据、规则、模型、标准库、安全校验。
- [x] 将 `SubmissionAnalysisService.generateAndStoreAnalysis` 中的 AI 流程迁移到 agent 编排层。
- [x] 为 agent 输出诊断路径摘要。
- [x] 支持模型失败时回退规则诊断。
- [x] 支持低置信度时标记 `NEEDS_MORE_EVIDENCE`。
- [x] 为 agent 编排增加单元/集成测试。

## Phase 3: 学习轨迹与个性化辅导

- [x] 学生轨迹接入细粒度错因。
- [x] 学生轨迹输出关注原因。
- [x] 教师概览接入细粒度高频问题和关注原因。
- [x] 识别阶段迁移：CE -> WA -> TLE -> AC。
- [x] 识别局部修复回退。
- [x] 识别频繁大改但错因不变。
- [x] 生成“这次比上次改善了什么”。
- [x] 支持同一作业内跨题能力摘要。

## Phase 4: 教师洞察与人工校正

- [x] 教师端展示每个高频问题的解释和建议干预方式。
- [x] 教师端展示“需关注学生”的证据链。
- [x] 增加 AI 置信度、不确定性、泄题风险展示。
- [x] 增加教师校正入口。
- [x] 将教师校正沉淀为 eval 候选样例。

## Phase 5: 多轮 AI 教练

- [x] 设计诊断后追问流程。
- [x] 对话引用诊断证据和学习轨迹摘要。
- [x] 对话完整引用证据包详情。
- [x] 对话受 hintPolicy 限制。
- [x] 默认使用苏格拉底式追问。
- [x] 对话结果沉淀为学习记录。
- [x] 学生回答后生成二次追问。
- [x] 学生端展示多轮追问历史。

## Phase 6: 跨题能力画像

- [x] 为题目维护知识点、算法策略、常见误区、边界类型。
- [x] 将错因映射到能力点。
- [x] 生成跨题学生能力画像。
- [x] 生成班级能力薄弱点。
- [x] 推荐下一道练习或复盘任务。

## Current Sprint

本轮目标：

- [x] 扩充 eval 到至少 10 条。
- [x] 抽出 `DiagnosticAgentService`。
- [x] 增加旧报告兼容测试。
- [x] 回写长期 spec 和本任务清单。

本轮增量：

- [x] 为 `DiagnosticAgentService` 增加编排测试。
- [x] 覆盖 `PARTIAL_FIX_REGRESSION`。
- [x] 将历史提交摘要接入证据包。
- [x] 开始识别“这次比上次改善了什么”。
- [x] 将 `diagnosticTrace` 写入诊断结果。
- [x] 支持模型增强异常时回退到规则诊断。

下一轮目标：

- [x] 为 `DiagnosisEvidencePackageBuilder` 增加单元测试。
- [x] 支持低置信度时标记 `NEEDS_MORE_EVIDENCE`。
- [x] 开始识别频繁大改但错因不变。
- [x] 扩展 eval 到 16 条可复跑 AI 诊断样例。
- [x] 输出当前 AI 诊断基线报告。
- [x] 教师端展示 AI 置信度、不确定性、泄题风险。

再下一轮目标：

- [x] 设计教师校正入口的最小闭环。
- [x] 支持同一作业内跨题能力摘要。
- [x] 将教师校正样例导出/汇总为可复跑 eval 数据。
- [x] 规划模型层 eval：有模型 key 时对 prompt / agent 输出做回归对比。

当前新增目标：

- [x] 题目元数据进入诊断证据包。
- [x] 教师校正 fixture 接入模型 eval。
- [x] 学生回答进入多轮 AI 教练记录。
- [x] AI 教练互动进入学生轨迹与教师概览。
- [x] 生成跨作业学生长期能力画像。
- [x] 教师概览展示班级能力薄弱点。
- [x] 基于能力画像推荐下一题或复盘任务。

下一步目标：

- [x] 将模型生成追问接入现有多轮教练 API，并保留规则回退。
- [x] 建设教师端 AI 质量面板。
- [x] 对新增学生端/教师端 AI 信息做体验二次打磨。

第二阶段目标：

- [x] 将模型追问质量纳入 coach eval fixture，覆盖证据引用、泄题风险和追问有效性。
- [x] 引入稳定学生身份，减少跨作业画像对姓名/学号启发式合并的依赖。
- [x] 为疑似重复学生画像提供人工合并/拆分工具。
- [x] 建设跨作业 AI 质量趋势后端 API。
- [x] 在教师/管理员端展示跨作业 AI 质量趋势。
- [x] 跨作业 AI 质量趋势按模型/诊断链来源版本拆分。
- [x] 记录推荐效果闭环。
- [x] 生成教师班级复盘建议。
- [x] 记录教师对课堂复盘建议的采纳、忽略和调整反馈。
- [x] 建立浏览器视觉 smoke 检查。
