# CombinedOutput

旧位置：`ExternalModelStagePayloads.CombinedOutput`。

旧用途：旧 single-call runtime 输出结构。

字段：

```text
diagnosisDecision: DiagnosisJudgeOutput
teachingHint: TeachingHintOutput
studentFeedback: StudentFeedback
```

当前状态：`ARCHIVED_ONLY`。
