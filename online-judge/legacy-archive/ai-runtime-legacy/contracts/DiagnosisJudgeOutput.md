# DiagnosisJudgeOutput

旧位置：`ExternalModelStagePayloads.DiagnosisJudgeOutput`。

旧用途：staged runtime 第一阶段输出，也被旧 single-call runtime 的 `CombinedOutput` 复用。

字段：

```text
primaryIssueTag
fineGrainedTag
evidenceRefs
primaryReasoning
secondaryIssues
distractorNotes
teachingPriority
improvementOpportunities
nextLearningAction
confidence
uncertainty
needsMoreEvidence
answerLeakRisk
```

当前状态：`ARCHIVED_ONLY`。
