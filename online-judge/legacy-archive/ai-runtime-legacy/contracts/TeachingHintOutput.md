# TeachingHintOutput

旧位置：`ExternalModelStagePayloads.TeachingHintOutput`。

旧用途：staged runtime 第二阶段输出，也被旧 single-call runtime 的 `CombinedOutput` 复用。

字段：

```text
studentHint
studentHintPlan
learningInterventionPlan
teacherNote
answerLeakRisk
```

当前状态：`ARCHIVED_ONLY`。
