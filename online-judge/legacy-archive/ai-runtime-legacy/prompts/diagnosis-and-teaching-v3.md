# diagnosis-and-teaching-v3

旧位置：`PromptTemplateRegistry.DIAGNOSIS_AND_TEACHING_V3`。

旧用途：旧 single-call runtime 主力版本，曾用于回归、质量评测和失败分类。

旧输出：

```text
CombinedOutput
```

旧问题：

- 无效 prompt version 曾静默回退到本版本。
- 会产生旧 stage：`DIAGNOSIS_AND_TEACHING`。

当前状态：`ARCHIVED_ONLY`。
