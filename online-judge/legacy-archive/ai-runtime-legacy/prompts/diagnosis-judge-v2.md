# diagnosis-judge-v2

旧位置：`PromptTemplateRegistry.DIAGNOSIS_JUDGE_V2`。

旧用途：staged runtime 的第一阶段，负责选择 `primaryIssueTag`、`fineGrainedTag`、证据和裁决置信度。

旧输入：

```text
ModelDiagnosisBrief
StandardLibraryPack
```

旧输出：

```text
DiagnosisJudgeOutput
```

当前状态：`ARCHIVED_ONLY`。
