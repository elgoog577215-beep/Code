# teaching-hint-v1

旧位置：`PromptTemplateRegistry.TEACHING_HINT_V1`。

旧用途：staged runtime 的第二阶段，基于已经校验的 `DiagnosisJudgeOutput` 生成学生提示、干预计划和教师说明。

旧输入：

```text
ModelDiagnosisBrief
StandardLibraryPack
DiagnosisJudgeOutput
```

旧输出：

```text
TeachingHintOutput
```

当前状态：`ARCHIVED_ONLY`。
