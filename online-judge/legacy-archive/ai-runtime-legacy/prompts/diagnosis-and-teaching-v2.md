# diagnosis-and-teaching-v2

旧位置：`PromptTemplateRegistry.DIAGNOSIS_AND_TEACHING_V2`。

旧用途：single-call runtime 迭代版本，保留旧 `diagnosisDecision + teachingHint` 结构。

旧输出：

```text
CombinedOutput
```

当前状态：`ARCHIVED_ONLY`。
