## 1. 诊断输出契约

- [x] 1.1 梳理现有 AI 诊断 DTO、mapper、validator 和前端消费字段，确认摘要、逐条建议、标签映射、成长候选的边界。
- [x] 1.2 为逐条建议增加或复用证据状态表达，覆盖有证据、无直接代码证据和证据无效三种状态。
- [x] 1.3 调整正式诊断 prompt，明确摘要不是逐条列表，修正/提升/成长候选必须独立数组返回。

## 2. 证据校验与后处理

- [x] 2.1 调整 `AdviceGenerationOutputValidator`，允许提升建议没有直接代码证据，但在提供 evidenceRefs 时必须校验有效。
- [x] 2.2 收紧 evidence alias 映射，避免泛化别名静默绑定到第一条代码或判题证据。
- [x] 2.3 调整 `AdviceGenerationFeedbackMapper`，保留 V2 多条建议，不再用 `studentReport` 或 `List.of(...)` 覆盖有效数组。
- [x] 2.4 调整 `StudentFeedbackAssembler` 与 `StudentFeedbackViewAssembler`，不再把全局 evidenceRefs 自动合并到每条 item。
- [x] 2.5 保留必要的旧数据降级路径，并在降级项中记录来源或 trace。

## 3. 标准库成长候选池

- [x] 3.1 调整标准库成长候选 DTO/实体/服务，使候选保存时由后端补齐来源题目、来源提交和诊断上下文。
- [x] 3.2 为成长候选保存 evidenceRefs 或无直接代码证据状态，避免丢失证据链。
- [x] 3.3 确认 `HIT` 不生成成长候选，`PARTIAL / MISS / OUT_OF_LIBRARY` 默认进入待审核候选池。
- [x] 3.4 增加重复候选聚合或保持现有聚合逻辑，并验证出现次数、最近来源和相似条目可追踪。

## 4. 运行时上下文与输出预算

- [x] 4.1 区分学生快反馈和教师深诊断 runtime profile，教师诊断使用标准或深度上下文。
- [x] 4.2 调整外部模型请求构造，兼容模式下尽力传递 provider 支持的输出预算或记录预算不可控 trace。
- [x] 4.3 检查正式诊断上下文包，确保教师深诊断保留足够代码、失败样例、规则信号和标准库结构。

## 5. 前端展示边界

- [x] 5.1 调整学生 AI 反馈展示，摘要、下一步行动和逐条建议分区展示，不重复渲染同一份代码证据。
- [x] 5.2 调整教师诊断结果展示，逐条呈现修正建议、提升建议、证据状态和标准库候选池来源。
- [x] 5.3 调整标准库人工治理视图，展示候选来源、出现次数、证据状态和相似条目。
- [x] 5.4 同步中英文 i18n 文案，并检查英文模式无中文残留。

## 6. 回归测试与验证

- [x] 6.1 增加 validator 测试：提升建议无证据可通过、无效证据不被绑定到第一条证据。
- [x] 6.2 增加 mapper/view 测试：多条建议保留、摘要不覆盖、全局证据不并入每条 item。
- [x] 6.3 增加成长候选测试：后端补齐来源、保存证据状态、`HIT` 不入池、`MISS` 可入池。
- [x] 6.4 增加 runtime 请求测试：教师深诊断使用正确 profile，兼容请求预算行为可验证。
- [x] 6.5 运行后端相关测试、前端构建或 lint、`openspec validate realign-ai-diagnosis-contract --strict` 和 `openspec validate --all`。

## 7. 记忆与收束

- [x] 7.1 将本次 AI 链路契约和候选池决策沉淀到 `docs/ai-memory/项目决策.md`。
- [x] 7.2 检查 git 工作区，只 stage 本次相关文件并提交。
