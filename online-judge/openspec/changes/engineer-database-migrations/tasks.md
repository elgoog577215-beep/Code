## 1. Flyway 基线与配置

- [x] 1.1 在真实 PostgreSQL 中从当前实体生成并审查完整 V1 Schema，覆盖全部生产表、约束和索引
- [x] 1.2 接入 Flyway 依赖和配置，使 `school` profile 启用迁移、默认禁止隐式基线并将 Hibernate 切换为 `validate`
- [x] 1.3 增加迁移命名与不可变规则，确保 H2 只作为非权威快速环境

## 2. 基线与结构门禁

- [x] 2.1 实现现有非空 PostgreSQL 的显式基线脚本，包含目标确认、关键结构检查、备份和迁移前计数
- [x] 2.2 实现 Schema readiness 检查，输出 Flyway 版本、失败记录、关键表、关键列和关键索引
- [x] 2.3 实现迁移前后关键表计数快照与对比，禁止无删除任务时出现异常下降

## 3. 备份与恢复能力

- [x] 3.1 将正式备份升级为 custom format，并生成归档可读性检查、SHA-256 和来源元数据
- [x] 3.2 强化恢复脚本，要求显式确认、先校验归档，并兼容历史 plain SQL 备份
- [x] 3.3 增加隔离恢复演练入口，确保不会连接或修改正式数据库 Volume

## 4. 验证与文档

- [x] 4.1 增加真实 PostgreSQL 自动验证，覆盖空库 V1、重复迁移、非空旧库基线和 Schema 漂移阻断
- [x] 4.2 增加部署脚本静态安全测试，防止绕过备份、基线确认或恢复确认
- [x] 4.3 更新学校部署文档、数据库迁移编写规则和环境变量说明
- [x] 4.4 运行全量测试、全新构建、Shell 语法检查、OpenSpec 严格校验和数据库恢复演练

## 5. 生产切换与沉淀

- [x] 5.1 在项目记忆中记录 PostgreSQL 为 Schema 权威、Flyway 负责演进、Hibernate 只负责校验的长期决策
- [ ] 5.2 生产切换前检查服务器资源、旧镜像、数据库 Volume 和现有未提交配置，创建并验证正式备份
- [ ] 5.3 对现有生产库执行一次性 V1 基线，核对前后计数、Flyway 历史、Schema readiness 和内容 readiness
- [ ] 5.4 外部构建并部署新应用镜像，只替换 app 容器，验证 readiness 后保留回滚证据
