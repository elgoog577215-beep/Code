## 1. 输出协议与模型对象

- [x] 1.1 定义诊断报告 v2 数据结构，包含执行门禁、命中状态、诊断锚点、学生三段报告、教师 trace 和标准库成长候选。
- [x] 1.2 将旧建议层输出映射改为兼容诊断报告 v2，同时保留现有学生端响应字段。
- [x] 1.3 增加提示开放度字段，支持 L1/L2/L3/L4 边界，其中默认学生端允许到 L3。

## 2. 搜索层改造

- [x] 2.1 扩展搜索层输出，支持 `HIT`、`PARTIAL`、`MISS` 三态。
- [x] 2.2 为每个候选补充召回理由、证据来源和不确定点。
- [x] 2.3 调整搜索层 prompt，让模型知道候选是导航图而不是强制答案。
- [x] 2.4 增加搜索层测试，覆盖精确命中、半命中和未命中。

## 3. 建议层改造

- [x] 3.1 新增 `diagnosis-report-v2` prompt，要求建议层可以确认、部分采用或否定搜索层候选。
- [x] 3.2 将学生可见反馈改为基础层、提高层、下一步行动三段自然语言。
- [x] 3.3 调整答案泄露规则，允许状态定义、小反例、手推方法，禁止完整代码、完整公式和完整答案。
- [x] 3.4 增加建议层测试，验证自然语言反馈比旧字段化输出更完整且不泄露答案。

## 4. 弹性校验与降级

- [x] 4.1 将校验结果分为硬失败和软修复。
- [x] 4.2 实现证据别名归一化，覆盖 `problemConstraints`、`sourceCode`、`judgeResult` 等常见别名。
- [x] 4.3 对未知标准库 ID 执行局部丢弃或转为库外发现，不整包回退。
- [x] 4.4 在 trace 中记录所有软修复、硬失败和降级原因。
- [x] 4.5 增加回归测试，确保 evidenceRef 小错误不再导致有效学生报告整包失败。

## 5. 标准库成长 Agent

- [x] 5.1 新增标准库成长候选数据结构，包含建议名称、归属路径、来源题目、来源提交、相似条目、变更理由和状态。
- [x] 5.2 实现成长 Agent 读取本地标准库相关条目后生成变更集。
- [x] 5.3 实现变更集预检，覆盖重复、冲突、路径缺失、来源缺失和置信度不足。
- [x] 5.4 默认将成长 Agent 结果写入候选池，供教师确认、合并、忽略或编辑。
- [x] 5.5 增加受控自动入库能力，仅在配置允许且门禁通过时写入正式标准库。
- [x] 5.6 为每次标准库写入记录变更前快照、差异说明和回滚信息。

## 6. 教师端与学生端展示

- [x] 6.1 学生端优先展示基础层、提高层、下一步行动三段自然语言。
- [x] 6.2 学生端弱化内部标签展示，只保留少量相关知识标签。
- [x] 6.3 教师端展示命中状态、标准库锚点、库外发现、软修复记录和扩库建议。
- [x] 6.4 教师端支持处理标准库成长候选：确认、合并、忽略、编辑。

## 7. 真实样本回归

- [x] 7.1 建立 8 个真实样本 fixture：两数求和、二维差分、单调栈重复值、石子合并、动态连通性、字符串窗口边界、滑窗调平、分层最短路。
- [x] 7.2 为每个样本写明期望错因、期望命中状态、学生反馈质量要求和是否应生成扩库候选。
- [x] 7.3 增加正式链路与快反馈对比断言。
- [x] 7.4 运行相关后端测试和 AI 诊断回归，记录仍未解决的模型或额度问题。

## 8. 配置与运维

- [x] 8.1 增加配置开关，控制诊断报告 v2、成长 Agent、自动入库和模型失败降级。
- [x] 8.2 在 readiness 或 trace 中暴露模型 429、超时、无效 JSON 和额度失败。
- [x] 8.3 更新部署说明，解释标准库成长 Agent 的权限、门禁和回滚方式。
