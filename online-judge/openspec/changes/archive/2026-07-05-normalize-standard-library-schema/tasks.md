## 1. 规范数据模型

- [x] 1.1 新增能力点、易错点、提升点、关系和旧映射领域实体
- [x] 1.2 新增对应仓储，并确保 code 或旧 layer/code 映射唯一

## 2. 种子同步与兼容迁移

- [x] 2.1 新增规范标准库同步器，从现有标准库种子生成新结构
- [x] 2.2 对旧 `SKILL_UNIT`、`MISTAKE_POINT`、`BASIC_CAUSE` 和 `IMPROVEMENT_POINT` 写入 legacy mapping
- [x] 2.3 同步过程不新增独立证据模式表，只保留轻量自然语言诊断提示

## 3. AI 读取路径接入

- [x] 3.1 调整 `AiStandardLibraryService`，让 AI 候选读取优先使用规范结构
- [x] 3.2 保留旧扁平表回退，避免规范结构为空时中断诊断链路

## 4. 测试与验证

- [x] 4.1 增加规范结构同步幂等、映射和分层关系测试
- [x] 4.2 增加标准库候选读取优先使用规范结构的测试
- [x] 4.3 运行 OpenSpec 校验和相关后端测试
