## 1. 规格与架构门禁

- [x] 1.1 校验 OpenSpec 变更，确保新增能力和修改能力可被 CLI 识别。
- [x] 1.2 固定默认链路：实时搜索 Agent 默认关闭，本地召回和单诊断 Agent 为主路径。
- [x] 1.3 清理或标记会让默认链路重新走双 Agent 的配置、测试或文档。

## 2. 学生复盘报告

- [x] 2.1 升级学生 AI 快反馈响应，增加学生可见 `studentReport`。
- [x] 2.2 调整学生 AI 快反馈 prompt：先讲人话，再引用证据，再给行动；保留结构化元数据。
- [x] 2.3 学生端结果页优先展示 `studentReport`，保留基础层、提高层和下一步行动兜底展示。
- [x] 2.4 增加测试：AI 成功时生成可读复盘，AI 不可用时不伪装成功。

## 3. 画像驱动推荐

- [x] 3.1 复用 `StudentAbilityProfileService` 的 `fineGrainedProfile` 和 `reviewCards`，不在推荐模块重复解析报告。
- [x] 3.2 优化推荐理由，让复盘卡片、细颗粒错因和能力点进入推荐理由或证据标签。
- [x] 3.3 增加测试：存在复盘卡片时，复盘/重做推荐能引用画像证据。

## 4. 教师班级洞察

- [x] 4.1 在作业/班级总览中聚合高频细颗粒错因和能力薄弱点。
- [x] 4.2 教师端展示班级高频错因、薄弱能力和建议讲评方向。
- [x] 4.3 增加测试：多名学生同类错因可聚合为教师端洞察。

## 5. 评测闭环

- [x] 5.1 确保评测报告保存学生实际可见 `studentReport`。
- [x] 5.2 固定失败分类并输出到 JSON 与 Markdown。
- [x] 5.3 增加或补齐 30 道典型高中信息学样例。

## 6. 标准库成长

- [x] 6.1 `OUT_OF_LIBRARY` 进入候选池，不实时改正式库。
- [x] 6.2 聚合同类候选，记录相似已有条目、证据摘要和出现次数。
- [x] 6.3 教师批准后入库，并触发 embedding 过期。

## 7. 验证与收束

- [x] 7.1 运行 `openspec validate complete-ai-review-personalization-loop --strict`。
- [x] 7.2 运行后端相关测试。
- [x] 7.3 若改动前端，运行 `npm run typecheck`。
- [x] 7.4 检查 `git status --short --branch`，提交本次任务相关改动。
