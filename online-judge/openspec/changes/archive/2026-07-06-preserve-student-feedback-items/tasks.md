## 1. 回归覆盖

- [x] 1.1 新增后端测试：模型返回多条建议并触发 runtime grounding 时，有效修正项和提升项仍被保留。
- [x] 1.2 新增后端测试：提高层漂移校正只替换跑偏提升项，不吞掉其他有效提升项。
- [x] 1.3 新增前端测试或可验证用例：摘要存在时，同一条建议和代码证据不会重复展示。

## 2. 后端修复

- [x] 2.1 调整 `enforceRuntimeEvidenceGrounding`，将越界主因作为可信主项合并，而不是整体覆盖所有结构化建议。
- [x] 2.2 调整 `alignIndexRuntimeImprovement`，只替换漂移提升项，并保留其他有效提升项。
- [x] 2.3 增加去重和合并辅助逻辑，避免后端生成的可信主项与模型原有项重复。

## 3. 前端修复

- [x] 3.1 调整学生反馈摘要和逐条列表渲染，避免第一条 item 的证据在摘要区和列表区重复出现。
- [x] 3.2 优化无代码证据展示边界，保证提升建议没有代码证据时不会误导为链路缺失。

## 4. 验证与收束

- [x] 4.1 运行学生快反馈后端测试。
- [x] 4.2 运行前端类型检查/构建或相关测试。
- [x] 4.3 运行 OpenSpec 校验并更新任务状态。
