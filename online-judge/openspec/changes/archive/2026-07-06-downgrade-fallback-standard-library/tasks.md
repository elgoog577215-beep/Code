## 1. 规划与规格

- [x] 1.1 创建兜底降级 OpenSpec proposal、design、spec 和任务清单。
- [x] 1.2 校验兜底降级 OpenSpec 变更。

## 2. 兜底层降级

- [x] 2.1 添加自动兜底条目识别与版本标记能力。
- [x] 2.2 标准库候选包和搜索定位默认优先返回手写智能条目，兜底仅作空结果后备。
- [x] 2.3 增加测试校验兜底条目仍保留但不再作为默认主力候选。

## 3. 低质量模板修复

- [x] 3.1 修复自动兜底生成器中的模板化易错点名称和定义。
- [x] 3.2 增加统计/质量测试，确保明显低质量模板数量归零。

## 4. 兜底素材继续吸收

- [x] 4.1 新增 V11 兜底吸收 seed，覆盖至少 8 个高价值兜底主题。
- [x] 4.2 为每个主题新增能力点、易错点和提升点，并避免复用兜底模板表达。
- [x] 4.3 将 V11 seed 接入 `AiStandardLibrarySeedCatalog` 并增加专项测试。

## 5. 验证与归档

- [x] 5.1 运行标准库 seed、规范化同步、候选包和质量相关测试。
- [x] 5.2 归档 OpenSpec 变更并再次运行全量 OpenSpec 校验。
