## 1. 配置

- [x] 1.1 将默认 AI 模型配置改为已验证可用的 Qwen 主模型，并保留环境变量覆盖能力。

## 2. 安全校验

- [x] 2.1 收紧 `ModelOutputSafetyPolicy` 的强拦截关键词，避免正常算法诊断词触发 `SAFETY_RISK`。
- [x] 2.2 保留直接答案、逐行改法、完整代码和可复制替换表达式的硬拦截。

## 3. Prompt 简化

- [x] 3.1 调整 `diagnosis-report-v2` 提示词，明确学生自然报告优先，后端 metadata 只用于审计和标准库成长。
- [x] 3.2 保留“建议条数由证据决定”和“标准库是参考规范包”的关键约束。

## 4. 测试与验证

- [x] 4.1 增加默认模型配置回归测试。
- [x] 4.2 增加安全校验误杀/泄露边界测试。
- [x] 4.3 增加 prompt 关键约束测试。
- [x] 4.4 运行 OpenSpec 校验、相关单元测试和 diff 检查。
