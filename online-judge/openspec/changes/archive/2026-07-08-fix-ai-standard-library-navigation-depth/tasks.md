## 1. 导航链路实现

- [x] 1.1 将标准库导航最大轮数从硬编码 3 改为默认 6 的可配置值
- [x] 1.2 在每轮导航视图中加入当前合法知识节点 code、诊断层 code 和最后一轮指令
- [x] 1.3 在展开分支前校验模型选择是否来自当前视图，并为非法分支提供一次修正机会
- [x] 1.4 保持导航失败关闭，不恢复本地召回或本地生成学生可见诊断

## 2. Prompt 与校验

- [x] 2.1 更新标准库导航 prompt，明确合法 code 列表、诊断层选择和最后一轮规则
- [x] 2.2 确认最终 `DONE` 仍经过标准库包与输出校验器，未知 ID 不被静默接受

## 3. 测试与验证

- [x] 3.1 增加多轮导航结构化测试，模拟根节点到知识点诊断层的完整路径
- [x] 3.2 增加非法分支修正测试，确认一次修正可恢复、修正失败会关闭
- [x] 3.3 运行相关后端测试、编译和 OpenSpec 严格校验
- [x] 3.4 用五道高质量样本重新验证真实模型链路并记录结果
  - 结果：2026-07-08 使用 `.env` 的 `OJ_MODELSCOPE_API_KEY` 并显式指定 `Qwen/Qwen3-235B-A22B-Instruct-2507` 跑 5 题，报告写入 `target/ai-simulation-reports/real-samples-website-vs-codex-20260708-182040.md`；当前被 ModelScope 今日配额 429 阻断，未能验证诊断质量。
