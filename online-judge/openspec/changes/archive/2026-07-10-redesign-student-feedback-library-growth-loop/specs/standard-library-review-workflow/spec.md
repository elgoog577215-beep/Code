## ADDED Requirements

### Requirement: 临时节点必须可治理且不伪装为正式节点
教师端和治理 API SHALL 区分临时节点与正式标准库节点，同时保留临时节点参与受限导航的能力。

#### Scenario: 查看临时节点来源
- **WHEN** 教师查看成长候选
- **THEN** 系统 SHALL 展示父知识节点、完整建议路径、节点 layer、来源提交、证据状态、置信度和出现次数

#### Scenario: 临时节点参与导航
- **WHEN** 候选尚未晋升但状态仍可审核
- **THEN** 系统 MAY 在对应知识点诊断层低优先级提供该候选
- **AND** 正式标准库浏览 SHALL 继续明确标识该节点为临时状态

#### Scenario: 拒绝临时节点
- **WHEN** 教师拒绝、忽略或候选被合并到其他节点
- **THEN** 该候选 SHALL 不再参与后续导航
- **AND** 系统 SHALL 保留审计记录
