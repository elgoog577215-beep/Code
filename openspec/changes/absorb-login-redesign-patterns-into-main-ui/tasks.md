## 1. 路由与入口

- [x] 1.1 重做 `/app` 入口页，突出学生、教师、公共题库三条路径。
- [x] 1.2 在 `App.tsx` 中新增 `/app/problems` 和 `/app/teacher/assignment/:id` 路由，并保持旧路径兼容。
- [x] 1.3 优化顶部导航，加入题库与邀请码入口，保持移动端可用。

## 2. 公共题库

- [x] 2.1 新增公共题库页面，使用现有 `api.problemCatalog()` 消费真实数据。
- [x] 2.2 实现关键词搜索、难度筛选、紧凑题目卡片和空状态。
- [x] 2.3 从题库卡片进入 `/app/problem/:id`，不使用 mock 数据。

## 3. 教师端拆页

- [x] 3.1 新增单作业详情页，展示邀请码、KPI、高频问题、学生列表和校正表单。
- [x] 3.2 在教师总览中加入进入单作业详情的动作，并保持课堂摘要优先。
- [x] 3.3 保留 AI 质量摘要降噪展示，不把工程状态重新提升到首屏。

## 4. 视觉与响应式

- [x] 4.1 在 `styles.css` 中新增入口、题库、作业详情和导航所需样式，并复用现有视觉系统。
- [x] 4.2 检查 390px、820px、1440px 下无横向溢出，主要按钮不低于 44px。

## 5. 验证

- [x] 5.1 运行前端类型检查。
- [x] 5.2 运行前端构建。
- [x] 5.3 运行关键页面浏览器 smoke，覆盖 `/app`、`/app/problems`、`/app/student`、`/app/teacher`、`/app/teacher/assignment/:id`、`/app/problem/:id`。
