## 1. OpenSpec 与范围

- [x] 1.1 写 proposal、design、spec 和任务清单，明确学生/教师固定入口地址。
- [x] 1.2 明确只新增学生身份和可见作业薄接口，不改判题、AI、评测和数据库结构。

## 2. 学生接口与会话

- [x] 2.1 新增学生班级登录请求 DTO 和后端接口。
- [x] 2.2 新增按学生画像读取本班可见作业接口。
- [x] 2.3 更新前端 API client、类型与 session storage。

## 3. 学生端路由与页面

- [x] 3.1 调整 `/app`、`/app/student`、`/app/student/login`、`/app/student/problems`、`/app/student/assignments/:assignmentId` 路由。
- [x] 3.2 重构学生首页为身份、公共题库、我的作业和下一步。
- [x] 3.3 新增学生登录页面，支持班级选择、姓名和学号/座号。
- [x] 3.4 新增学生作业详情页，展示多题列表、完成状态和下一题。

## 4. 教师端与导航边界

- [x] 4.1 调整顶部导航：学生上下文不展示教师管理入口，教师上下文不展示学生作业入口。
- [x] 4.2 保留旧邀请码和旧路由兼容，但不把邀请码作为主导航。
- [x] 4.3 输出本地访问地址时区分学生地址和教师地址。

## 5. 视觉与验证

- [x] 5.1 补齐学生首页、登录、作业详情和公共题库的响应式样式。
- [x] 5.2 更新 browser smoke mock 与断言，覆盖学生未登录、登录、作业详情、教师端。
- [x] 5.3 运行 `npm run typecheck` 或等价 TypeScript 检查。
- [x] 5.4 运行前端 build。
- [x] 5.5 运行 browser smoke；如可用，浏览器检查学生端和教师端地址。

## 6. PTA 式学生做题链路二次收敛

- [x] 6.1 将 `/app/student` 收敛为作业入口和右上角登录/身份，不再展示分散的身份卡、下一步卡和题库预览。
- [x] 6.2 新增 `/app/student/assignments/public` 公开作业入口，并让 `/app/problems`、`/app/student/problems` 兼容跳转。
- [x] 6.3 将 `/app/student/assignments/:assignmentId` 改为自动进入下一道题的作业入口。
- [x] 6.4 新增 `/app/student/assignments/:assignmentId/problems/:problemId` 三栏做题工作台。
- [x] 6.5 将提交反馈从常驻右栏迁移到结果弹窗，包含测试点、错误提示、优化提示和 Coach 下一问。
- [x] 6.6 更新响应式样式和 browser smoke 断言，覆盖公共题库、老师作业、提交弹窗和关闭后的查看结果入口。

## 7. 学生端多尺寸与操作顺手度优化

- [x] 7.1 修复“我的作业”导航锚点，确保学生首页内部跳转有真实落点。
- [x] 7.2 切换题目时清空上一题提交结果和 Coach 状态，避免跨题残留。
- [x] 7.3 移动端题面下方新增“去写代码”入口，减少读题到编辑器的滚动成本。
- [x] 7.4 优化 820px 平板工作台为题单横排 + 题面/代码两栏。
- [x] 7.5 压缩学生首页和做题页顶部高度，减少首屏无效留白。
