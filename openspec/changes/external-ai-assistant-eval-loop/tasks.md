## 1. 评测规格与样本

- [x] 1.1 新增统一 AI 助手评测 fixture，覆盖提交诊断、Coach 追问、成长报告。
- [x] 1.2 为每条样本补充老师期望、评分点、禁止行为、教学价值说明。

## 2. 评测运行器与报告

- [x] 2.1 新增 fixture loader 和报告模型。
- [x] 2.2 新增 live eval 测试入口，直接调用项目后端 AI 服务。
- [x] 2.3 报告中区分运行失败、质量失败、安全失败和调用兜底。

## 3. 真实测试与迭代

- [x] 3.1 运行无 key 结构测试，确保评测集质量稳定。
- [x] 3.2 使用外部模型运行 live smoke eval。
- [x] 3.3 基于评测结果做一轮针对性优化。
- [x] 3.4 重新运行相关测试并记录结果。

## 4. 第二轮：复杂真实样本与严格门槛

- [x] 4.1 将提交诊断 fixture 升级为中等长度真实错误代码，覆盖多次输入、状态重置、DP 状态设计、贪心假设等错因。
- [x] 4.2 收紧 live eval 的证据命中规则：老师指定 `requiredEvidenceRefs` 时必须命中指定证据。
- [x] 4.3 根据复杂样本补强提示词与标准库决策协议，减少泛化标签和无根据的 `NEEDS_MORE_EVIDENCE`。
- [x] 4.4 运行结构测试与外部模型 live eval，记录第二轮结果。

## 5. 第三轮：高颗粒度复杂样本扩展与定向抽测

- [x] 5.1 新增 6 条中等长度提交诊断 fixture，覆盖过度模拟、极小输入、原地状态推进、输出格式、样例过拟合、最大规模边界等错因。
- [x] 5.2 为 live eval 增加 `AI_EVAL_CASE_IDS` 定向过滤能力，支持只抽测新增样本或失败样本。
- [x] 5.3 根据新增错因补强提示词、标准库决策协议和安全拦截规则，降低外部模型直接给公式、隐藏数据或替换结构的概率。
- [x] 5.4 运行结构测试、OpenSpec 校验和外部模型定向 live eval，并根据结果继续迭代。
- [x] 5.5 将 live eval 发现的“标签正确但教学动作过泛”问题纳入质量门槛，并在运行时把明确错因对齐到具体教学动作。
