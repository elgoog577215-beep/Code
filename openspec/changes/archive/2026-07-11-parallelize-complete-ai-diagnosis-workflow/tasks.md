## 1. 持久化编排基础

- [x] 1.1 新增诊断 Run、阶段执行记录、状态枚举、索引和 Repository，并覆盖正式版本递增与阶段唯一性测试
- [x] 1.2 新增诊断工作流状态服务，支持创建/复用 Run、阶段 claim、成功输出复用、失败记录、完成收口和学生进度映射
- [x] 1.3 新增专用 AI 并行执行器与恢复扫描器，支持遗留运行中阶段恢复和未完成 Run 重新入队

## 2. 单核心诊断与并行 AI 分支

- [x] 2.1 将核心自由诊断接入持久化阶段，保证同一 Run 只生成一次规范 issues 并可从阶段输出恢复
- [x] 2.2 将不同 issue 的标准库逐层挂接改为受控并行执行，保存每个 issue 的阶段状态并按原顺序聚合
- [x] 2.3 新增教师洞察结构化输出分支，与学生建议分支并行执行且共同引用规范 issues 和证据
- [x] 2.4 将阶段失败、局部重试和已成功输出复用接入正式分析收口，不使用规则或技术性文案伪造成功

## 3. 正式保存与派生投影

- [x] 3.1 将正式分析、学生反馈修订和 Run 版本关联起来，保证历史版本追加保存和兼容旧记录读取
- [x] 3.2 将诊断事实/生命周期、教师统计、标准库成长候选和运行质量拆为可独立重试的幂等投影阶段；事实、教师统计与运行质量在正式保存后并行，成长候选在完整输出校验后独立执行
- [x] 3.3 增加投影失败不回滚正式诊断、重复执行不放大统计和启动恢复的回归测试

## 4. 学生端进度体验

- [x] 4.1 扩展学生反馈查询 DTO/API，返回学习化诊断阶段、完成数量、总数量、重试状态和更新时间
- [x] 4.2 在题目页展示完整诊断进度与恢复状态，移除学生可见 API/provider/token 技术措辞
- [x] 4.3 同步中英文翻译并验证桌面、移动端和英文模式的生成中、重试、失败与完成状态

## 5. 验证与交付

- [x] 5.1 运行后端阶段编排、并发、恢复、版本、投影和兼容性测试，并通过完整 Maven 测试
- [x] 5.2 运行前端类型检查、构建和针对进度状态的界面验证
- [x] 5.3 使用同一组真实困难样本对比改造前后问题完整性、关键路径耗时、并行证据和失败恢复能力
- [x] 5.4 运行 `openspec validate --all`、`git diff --check`，更新项目决策记忆并归档变更
