## 1. 基线与数据结构

- [x] 1.1 记录现有提交、分析、反馈、反馈事件和教师概览的基线样本，覆盖具名课堂提交、匿名课堂历史、公共练习、成功反馈和失败反馈
- [x] 1.2 新增反馈版本实体、仓储、唯一幂等键和按提交查询索引，并保持 H2 与 Postgres 字段类型兼容
- [x] 1.3 新增逐问题诊断事实实体、仓储、分析版本关联、路径字段、校正关联、投影状态和教师聚合索引
- [x] 1.4 为现有当前反馈快照增加最新版本关联或等价兼容机制，保证旧 `/student-ai-feedback` 查询不破坏
- [x] 1.5 增加数据结构测试，验证反馈多版本、同一投影重放不重复、正式/临时/推断/未归类路径可保存

## 2. 课堂提交身份与上下文完整性

- [x] 2.1 实现后端课堂提交上下文解析器，从学生令牌取得身份并校验学生班级、作业班级和作业题目关系
- [x] 2.2 调整提交接口，使课堂提交忽略前端对最终学生身份的决定权，并拒绝身份不一致、跨班作业和题目不属于作业的请求
- [x] 2.3 保留公共题库登录提交与匿名练习路径，确保匿名记录不进入课堂学生统计和个人画像
- [x] 2.4 增加课堂提交强校验开关及可观测拒绝原因，为宽松路径到强校验的分阶段切换提供回滚能力
- [x] 2.5 增加提交鉴权与上下文测试，覆盖合法提交、伪造学生 ID、无令牌课堂提交、跨班提交、错题目和匿名公共练习

## 3. 反馈版本与诊断事实双写

- [x] 3.1 在提交评测完成后同步建立可恢复的反馈待处理或生成中记录，验证进程退出前数据库已有状态
- [x] 3.2 在反馈成功、失败和重试终态追加保存完整反馈版本、失败原因、模型/提示词/结构版本和生成时间
- [x] 3.3 实现完整分析到逐问题诊断事实的投影器，逐条保存稳定问题 ID、层级、主要问题、知识路径、标准库归属、证据和置信度
- [x] 3.4 将诊断事实投影接入分析保存链路，保证完整报告与事实投影的一致性，并为投影失败提供可重试状态
- [x] 3.5 将教师校正作为独立覆盖层接入有效诊断事实读取，保留原始 AI 值、修正值和修正来源
- [x] 3.6 扩展反馈查看事件以关联反馈版本，并保持后台预取不记查看、真实打开幂等记录首次查看时间
- [x] 3.7 增加双写回归测试，覆盖多问题、多次重试、失败后成功、投影重放、教师校正和旧反馈兼容读取

## 4. 历史数据幂等回填

- [x] 4.1 实现可分批、可续跑、可重复执行的反馈版本和诊断事实回填服务，记录批次、游标、成功、跳过和失败原因
- [x] 4.2 从正式标准库 ID 和后端 breadcrumb 回填可靠知识路径，对无法恢复的记录标记 `INFERRED` 或 `UNCLASSIFIED`
- [x] 4.3 识别带作业但缺学生身份的历史提交，保留原记录并标记身份缺失，禁止自动猜测绑定
- [x] 4.4 增加回填 dry-run 或等价只读预览，输出身份缺失、诊断缺失、路径未归类和版本缺失统计
- [x] 4.5 使用历史 fixture 连续执行两次回填，验证版本数、事实数和事件关联数不重复增加

## 5. 学生最近结果与历史连续性

- [x] 5.1 新增“学生 + 作业 + 题目”和“学生 + 无作业 + 题目”的历史仓储查询，保留匿名公共练习隔离范围
- [x] 5.2 扩展历史摘要 API，返回评测摘要、分析状态、反馈状态、当前反馈版本和数据完整性状态，并执行提交访问校验
- [x] 5.3 在学生题目页首次加载时恢复当前范围最近提交详情和保存反馈，仅对未请求或过期生成状态继续轮询
- [x] 5.4 在学生题目页新增时间倒序历史入口，允许重新打开每次提交的评测、代码、测试点和对应反馈版本
- [x] 5.5 保证打开历史 `READY` 或终态失败反馈不会重新生成，只有明确重试失败反馈时才触发新版本
- [x] 5.6 为最近结果、历史列表、反馈状态、身份错误和空状态补齐中英文翻译，不在组件内写死新增文案
- [x] 5.7 增加学生历史单元测试、组件测试和浏览器测试，覆盖刷新恢复、跨作业隔离、公共练习隔离、历史打开、反馈查看事件和中英文布局

## 6. 后端教师证据聚合

- [x] 6.1 定义统一教师聚合 DTO，固定在册人数、提交人数、尝试次数、学生通过率、尝试通过率和数据完整率字段
- [x] 6.2 实现章节、知识点、能力点和易错点路径桶聚合，直接读取诊断事实与教师校正，不解析标签关键词猜路径
- [x] 6.3 实现错误出现次数、涉及学生数、重复错误学生数、涉及题目数及其去重口径
- [x] 6.4 实现同题相邻提交的问题集合比较，生成等待后续、改善、仍卡同类、问题转移、回退、评测阶段变化和暂无明确变化
- [x] 6.5 实现恢复率与反馈查看后变化聚合，同时返回分子、分母、样本数和前后提交证据，排除无后续样本
- [x] 6.6 实现学生近期状态聚合，限定时间窗口并返回独立提交数、涉及题目数、重复问题、最近变化和证据不足状态
- [x] 6.7 为班级、作业、题目和学生详情提供一致的聚合查询或接口，保证逐级下钻只改变范围不改变口径
- [x] 6.8 增加聚合测试，覆盖班级有未提交学生、一人多次提交、跨题重复、未诊断提交、匿名课堂历史和教师校正

## 7. 教师教学分析看板接入

- [x] 7.1 更新教师分析前端类型和 API 客户端，接入后端路径桶、精确分母、变化状态、学生近期状态和数据完整性
- [x] 7.2 删除教师前端的关键词章节/能力路径推断和重复聚合逻辑，图表只消费后端返回结果
- [x] 7.3 更新班级分析页，展示在册/提交/未提交、学生与尝试通过率、高频路径、涉及学生、重复错误和作业下钻
- [x] 7.4 更新作业分析页，展示题目进度矩阵、错因范围、数据完整性、恢复样本和题目下钻
- [x] 7.5 更新题目分析页，展示错误分布、路径状态、涉及学生、反馈后变化和可打开的提交证据
- [x] 7.6 增加学生近期状态详情，展示提交时间线、问题变化、已恢复证据、重复问题和证据不足，不使用固定能力标签
- [x] 7.7 保持教师 AI 文案为客观归因和证据表达，移除替教师做决定的动作语言，并为新增内容同步中英文翻译
- [x] 7.8 增加教师看板组件与浏览器测试，验证班级/作业/题目下钻、四种路径状态、空数据、缺失数据、中英文、明暗主题和响应式布局

## 8. 一致性验证与切换

- [x] 8.1 建立新旧聚合样本对比工具，解释口径差异并核对代表班级、作业、题目和学生的证据提交
- [x] 8.2 在当前 H2 数据上运行回填预览和正式回填，确认现有匿名课堂提交被标记为身份缺失且不进入学生画像
- [x] 8.3 运行后端针对性测试、完整 Maven 测试、前端 typecheck、生产构建和 `git diff --check`
- [x] 8.4 运行学生与教师端 visual smoke，检查手机、平板、桌面、明暗主题和中英文模式，无中文残留、遮挡或不可点击证据
- [x] 8.5 验证提交后刷新仍能恢复结果、历史反馈不被无条件重跑、教师错误计数可下钻复核、样本不足不展示误导百分比
- [x] 8.6 执行 `openspec validate --all`，记录新读取路径和课堂强校验开关的启用与回滚方式
