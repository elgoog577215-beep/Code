## 1. 安全判定逻辑

- [x] 1.1 调整 `ModelOutputValidator`，让顶层 `teachingHint.answerLeakRisk` 不再单独触发 `SAFETY_RISK`。
- [x] 1.2 确认学生可见字段中的高风险等级和泄题文本仍会触发拒绝。
- [x] 1.3 调整 `AiReportService` 运行时风险合成，优先使用学生可见提示计划和干预计划风险。

## 2. 回归测试

- [x] 2.1 增加验证器测试：顶层风险为高但学生可见内容安全时应通过。
- [x] 2.2 增加运行时测试：输入解析安全提示不应因教师备注提到禁用写法而降级。
- [x] 2.3 增加安全层测试：教师备注或报告代码定位不应污染学生可见安全判断。

## 3. 验证与沉淀

- [x] 3.1 运行相关 Maven 测试和 OpenSpec 严格校验。
- [x] 3.2 小规模运行外部模型 live eval，验证安全误判是否减少；如果被配额限制，需要在报告中区分容量问题和质量问题。
- [x] 3.3 将本次“学生可见安全边界”的经验写入项目记忆。
