## 1. 发布入口收束

- [x] 1.1 将 GitHub Actions 部署工作流改为仅 `workflow_dispatch`，并传递 `--confirm-build`
- [x] 1.2 新增受版本控制的服务器部署脚本，复用显式构建和 `--no-build` 安全启动入口
- [x] 1.3 更新部署文档，明确 main push 不再自动上线和人工发布步骤

## 2. 自动门禁

- [x] 2.1 扩展部署脚本安全测试，覆盖工作流触发条件、确认参数和服务器入口绕行路径
- [x] 2.2 运行 Shell 语法、定向测试、OpenSpec 严格校验和差异检查

## 3. 生产切换与验收

- [ ] 3.1 备份服务器旧部署入口，安装仓库脚本链接，并验证无确认参数时零副作用失败
- [ ] 3.2 推送变更并确认 GitHub 未创建新的 push 部署任务
- [ ] 3.3 复验应用、Flyway、数据库容器、Volume、readiness 和题库接口，记录发布竞态经验
