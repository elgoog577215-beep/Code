## 1. OpenSpec 规划

- [x] 1.1 创建 `rebuild-education-agent-core` 变更。
- [x] 1.2 编写 proposal，明确本变更不是修补 OJ + AI 报告，而是重建教育 agent 核心主链路。
- [x] 1.3 编写 design，定义四层架构、复用/退出策略、三层验收体系、迁移计划和失败边界。
- [x] 1.4 编写 spec，覆盖架构、学生 AI 快反馈、学习轨迹、教师洞察、验收体系和迁移约束。
- [x] 1.5 运行 OpenSpec 状态检查和 strict validation。

## 2. P0 目标架构和接口契约

- [x] 2.1 盘点当前后端服务和 DTO，标记判题事实、学生反馈、学习轨迹、教师洞察分别可复用的资产。
- [x] 2.2 定义 `JudgeFacts` 契约，包含 verdict、测试点、首个失败点、错误、代码证据、证据引用。
- [x] 2.3 定义 `StudentAiFeedback` 契约，包含 status、source、repairItems、improvementItems、nextQuestion、latency、安全状态。
- [x] 2.4 定义学生 AI 快反馈状态机：`NOT_REQUESTED`、`GENERATING`、`READY`、`TIMEOUT`、`FAILED`、`SAFETY_REJECTED`。
- [x] 2.5 定义学生反馈 quality rubric，覆盖修正建议、提升建议、追问、证据扎根、安全边界。
- [x] 2.6 定义观测指标事件：latency、结构化成功、超时、安全拒绝、反馈查看、反馈后提交。
- [x] 2.7 明确旧 `SubmissionAnalysis` 在 P0-P4 的兼容角色，禁止继续作为学生建议主契约。

## 3. P1 学生提交后的 AI 快反馈链路

- [x] 3.1 新增或重组后端服务边界，让学生 AI 快反馈独立于完整教师诊断生成。
- [x] 3.2 模型输入只包含判题事实、必要题目约束、代码证据、候选信号和安全规则。
- [x] 3.3 模型输出直接对齐 `StudentAiFeedback`，不再要求从长报告中抽取学生三栏。
- [x] 3.4 本地规则只参与证据整理、候选信号和输出校验，不生成学生端 repair/improvement 文案。
- [x] 3.5 前端结果弹窗只展示 `source=MODEL` 且通过安全校验的修正建议和提升建议。
- [x] 3.6 AI 超时、失败、结构化失败、安全拒绝时返回明确状态，前端不得一直显示“正在分析”。
- [x] 3.7 增加学生 AI 快反馈后端测试、模型 fixture 测试和 browser smoke。

## 4. P2 学习轨迹

- [x] 4.1 建立同题多次提交轨迹信号，识别失败推进、回退、AC 后失败。
- [x] 4.2 建立同类错因复发信号，区分单题卡住和跨题误区。
- [x] 4.3 建立 AI 反馈后行为信号，记录学生是否查看、是否继续提交、是否改善。
- [x] 4.4 将学习轨迹输出给学生推荐和教师洞察，不直接堆到学生做题页。
- [x] 4.5 增加轨迹单元测试和半真实提交链 fixture。

## 5. P3 教师课堂洞察

- [x] 5.1 重建教师首页信息架构：作业选择、课堂 KPI、需关注学生、高频问题、共性误区。
- [x] 5.2 AI 质量只作为辅助证据展示，不压过课堂过程。
- [x] 5.3 工程状态和 runtime 术语进入系统详情或折叠区，并翻译成教师可理解语言。
- [x] 5.4 教师校正入口与 AI 诊断/质量评测闭环连接。
- [x] 5.5 增加教师端 browser smoke 和关键数据聚合测试。

## 6. P4 旧链路清理和主流程收敛

- [x] 6.1 将旧 `SubmissionAnalysis` 大对象降级为兼容层或归档层，移除其对学生端主展示的控制权。
- [x] 6.2 清理学生端旧入口、重复导航和解释性废话。
- [x] 6.3 清理教师端工程术语堆叠和旧 AI 报告入口。
- [x] 6.4 更新 API client、types、format、browser smoke，确保新主链路可验证。
- [x] 6.5 运行 typecheck、build、后端相关测试、OpenSpec validation、browser smoke。

## 7. 完成审计

- [x] 7.1 对照 spec 逐条确认布尔门禁是否成立。
- [x] 7.2 用 rubric 评审代表性学生失败提交、通过提交、教师课堂场景。
- [x] 7.3 汇总观测指标是否已经记录，未设置阈值的指标需说明原因。
- [x] 7.4 确认旧代码未被无意回滚，未提交无关工作区改动。
- [x] 7.5 输出剩余风险、迁移缺口和下一阶段建议。
