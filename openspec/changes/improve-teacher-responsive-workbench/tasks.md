## 1. OpenSpec

- [x] 1.1 创建 `improve-teacher-responsive-workbench` 变更。
- [x] 1.2 写入 proposal、design、spec 和 tasks。
- [x] 1.3 运行 OpenSpec 校验。

## 2. 教师端布局

- [x] 2.1 调整教师管理页容器和任务网格，覆盖桌面三列、中屏两列、手机单列。
- [x] 2.2 调整作业中心桌面容器和作业行列宽，保持行式比较。
- [x] 2.3 调整作业详情为桌面工作台布局，中屏保留学生内部两列，手机单列。
- [x] 2.4 检查深色主题下新增布局背景和边框仍可读。

## 3. 验证

- [x] 3.1 更新 `browser-smoke` 响应式断言。
- [x] 3.2 运行 `npm run typecheck`。
- [x] 3.3 运行 `npm run smoke:browser`。
- [x] 3.4 根据失败结果迭代修正，直到关键验收通过或说明限制。
