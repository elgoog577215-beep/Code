## 1. OpenSpec

- [x] 1.1 编写 proposal、design、spec 和 tasks。
- [x] 1.2 运行 `openspec validate surface-transport-attribution-teacher-workbench --strict`。

## 2. 教师端展示

- [x] 2.1 增加 transport telemetry 的前端格式化 helper。
- [x] 2.2 在作业级 AI 质量“模型归因”区域展示 transport mode 和 stream 计数。
- [x] 2.3 在跨作业“来源质量”片段展示 transport telemetry。
- [x] 2.4 补充紧凑 chip 样式与响应式兼容。

## 3. 验证

- [x] 3.1 运行前端 typecheck。
- [x] 3.2 运行 OpenSpec strict 校验。
- [x] 3.3 运行 `git diff --check`。
- [x] 3.4 如本地服务可用，做教师工作台 smoke 验证（已完成 Vite + fake API HTTP smoke；浏览器页面验证受 Browser URL policy 限制）。
